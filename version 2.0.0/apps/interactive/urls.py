from django.urls import path
from . import views

app_name = 'interactive'

urlpatterns = [
    # Rutas para experiencias interactivas
    # path('mapa/', views.interactive_map, name='map'),
    # path('realidad-aumentada/', views.augmented_reality, name='ar'),
]