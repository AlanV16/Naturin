from django.urls import path
from . import views

app_name = 'gamification'

urlpatterns = [
    # Rutas para gamificación
    # path('logros/', views.achievements, name='achievements'),
    # path('recompensas/', views.rewards, name='rewards'),
]