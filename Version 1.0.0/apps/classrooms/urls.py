from django.urls import path
from . import views

app_name = 'classrooms'

urlpatterns = [
    # Rutas para aulas virtuales
    # path('aula/<int:classroom_id>/', views.classroom_detail, name='classroom_detail'),
    # path('mis-aulas/', views.my_classrooms, name='my_classrooms'),
]